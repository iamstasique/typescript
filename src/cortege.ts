// CORTEGE - is an array with a fixed size and a known set of data

let array: [string, number, boolean] = ['Stas', 1, true];
// array[0] = 100; // Type 'number' is not assignable to type 'string'.ts(2322)
